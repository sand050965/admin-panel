<script setup>
import {useDrawerStore} from "@/stores/drawer";
import {ref} from "vue";

const drawerStore = useDrawerStore();

const links = ref([
  ['fa:fas fa-tachometer-alt', 'Overview', '/dashboard'],
  ['fa:fas fa-user', 'Admin Account', '/account'],
  ['fa:fas fa-users', 'User Management', '/user'],
  ['fa:fas fa-newspaper', 'Post Management', '/post'],
  ['fa:fas fa-tags', 'Tag Management', '/tag'],
]);
</script>

<template>
  <v-navigation-drawer
      class="bg-grey-lighten-3 drawer"
      v-model="drawerStore.drawer"
      permanent
  >
    <v-container
        class="h-100 w-100"
        rounded="lg"
    >
      <v-sheet
          class="pa-4 h-100"
          rounded="lg"
      >

        <v-list-item class="w-100">
          <template v-slot:prepend>
            <v-avatar color="info">
              <v-icon icon="mdi-account-circle"></v-icon>
            </v-avatar>
          </template>

          <v-list-item-title>Evan You</v-list-item-title>

          <v-list-item-subtitle class="text-wrap">john12kjkbkbjb345@google.com</v-list-item-subtitle>

        </v-list-item>

        <v-divider class="my-2"></v-divider>

        <v-list rounded="lg">
          <v-list-item
              v-for="[icon, text, target] in links"
              :key="icon"
              link
              color="bg-grey-lighten-3"
              :to="target"
          >
            <template v-slot:prepend>
              <v-icon>{{ icon }}</v-icon>
            </template>

            <v-list-item-title>{{ text }}</v-list-item-title>
          </v-list-item>
        </v-list>

      </v-sheet>

    </v-container>
  </v-navigation-drawer>
</template>

<style scoped lang="scss">
.drawer {
  width: 360px !important;
}

.text-wrap {
  overflow-wrap: break-word;
}
</style>